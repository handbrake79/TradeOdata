<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.sk42.tradeodata">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.SettingsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.PREFERENCE" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Document.DocumentActivity">
        </activity>
        <activity
            android:name=".Activities.Documents_List.DocList_Activity"
            android:label="@string/title_activity_documents__list_"
            />

        <service
            android:name=".Services.CommunicationWithServer"
            android:exported="false" />

        <activity android:name=".Activities.ProductsListBrowser.ProductsListActivity" />
        <activity android:name=".Activities.Document.QtyInputActivity"></activity>
    </application>

</manifest>